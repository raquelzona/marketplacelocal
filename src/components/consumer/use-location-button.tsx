"use client";import {useState} from "react";import {usePathname,useRouter,useSearchParams} from "next/navigation";
export function UseLocationButton(){const [status,setStatus]=useState("");const router=useRouter(),path=usePathname(),current=useSearchParams();function locate(){if(!navigator.geolocation)return setStatus("Localização não disponível neste navegador.");setStatus("Obtendo localização…");navigator.geolocation.getCurrentPosition(({coords})=>{const params=new URLSearchParams(current);params.set("lat",coords.latitude.toFixed(6));params.set("lng",coords.longitude.toFixed(6));params.set("pagina","1");setStatus("Localização usada somente nesta busca.");router.push(`${path}?${params}`)},()=>setStatus("Não foi possível acessar sua localização."),{enableHighAccuracy:false,timeout:10000,maximumAge:300000})}return <div><button type="button" onClick={locate} className="button-secondary">Usar minha localização</button>{status&&<p role="status" className="mt-2 text-xs text-slate-500">{status}</p>}</div>}
