"use client";
import Link from "next/link";import {usePathname} from "next/navigation";import {logoutAction} from "@/app/auth/actions";
const items=[['/admin','Visão geral'],['/admin/comerciantes','Comerciantes'],['/admin/consumidores','Consumidores'],['/admin/produtos','Produtos'],['/admin/questionarios','Questionários'],['/admin/campanhas','Campanhas'],['/admin/origens','Origens'],['/admin/metricas','Métricas'],['/admin/auditoria','Auditoria']] as const;
export function AdminNav(){const path=usePathname();return <nav aria-label="Administração" className="sticky top-[72px] z-40 border-b border-slate-200 bg-white/95 backdrop-blur-xl"><div className="container-shell flex h-16 items-center gap-1 overflow-x-auto">{items.map(([href,label])=>{const active=href==='/admin'?path===href:path.startsWith(href);return <Link key={href} href={href} aria-current={active?'page':undefined} className={`shrink-0 rounded-full px-3 py-2 text-xs font-semibold sm:text-sm ${active?'bg-slate-900 text-white':'text-slate-500 hover:bg-slate-50 hover:text-slate-900'}`}>{label}</Link>})}<form action={logoutAction} className="ml-auto shrink-0"><button className="rounded-full px-3 py-2 text-sm font-semibold text-slate-500 hover:bg-red-50 hover:text-red-700">Sair</button></form></div></nav>}
