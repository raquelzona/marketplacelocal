"use client";import {useActionState} from "react";import {createCampaignAction,type CampaignAdminState} from "@/app/admin/campanhas/actions";
export function CampaignForm({questionnaires}:{questionnaires:{id:string;titulo:string}[]}){const [state,action,pending]=useActionState(createCampaignAction,{} as CampaignAdminState);return <form action={action} className="mt-5 grid gap-4 sm:grid-cols-2"><label className="sm:col-span-2 text-sm font-semibold">Título<input required name="title" className="consumer-input mt-2"/></label><label className="sm:col-span-2 text-sm font-semibold">Descrição<textarea name="description" maxLength={1200} className="mt-2 min-h-24 w-full rounded-xl border border-slate-300 p-3"/></label><label className="text-sm font-semibold">Questionário<select required name="questionnaire_id" className="consumer-input mt-2"><option value="">Selecione</option>{questionnaires.map(q=><option key={q.id} value={q.id}>{q.titulo}</option>)}</select></label><label className="text-sm font-semibold">Slug (opcional)<input name="slug" placeholder="pesquisa-do-bairro" className="consumer-input mt-2"/></label><label className="text-sm font-semibold">Cidade alvo<input name="target_city" className="consumer-input mt-2"/></label><label className="text-sm font-semibold">Bairro alvo<input name="target_neighborhood" className="consumer-input mt-2"/></label><label className="text-sm font-semibold">Início<input required type="datetime-local" name="starts_at" className="consumer-input mt-2"/></label><label className="text-sm font-semibold">Término<input required type="datetime-local" name="ends_at" className="consumer-input mt-2"/></label><label className="sm:col-span-2 flex items-center gap-2 text-sm"><input type="checkbox" name="active"/> Ativar ao criar</label>{(state.error||state.success)&&<p role="status" className={`sm:col-span-2 text-sm ${state.error?"text-red-700":"text-teal-700"}`}>{state.error??state.success}</p>}<button disabled={pending} className="button-primary sm:col-span-2">{pending?"Criando…":"Criar campanha"}</button></form>}
