import type {Metadata} from 'next';import {createClient} from '@/lib/supabase/server';
export const metadata:Metadata={title:'Painel administrativo'};
const labels:Record<string,string>={consumers:'Consumidores',merchants:'Comerciantes',pending_merchants:'Lojas pendentes',products:'Produtos',searches:'Buscas registradas',active_questionnaires:'Pesquisas ativas',submissions:'Respostas concluídas',active_cities:'Cidades ativas'};
export default async function Page(){const supabase=await createClient();const {data,error}=await supabase.rpc('get_admin_dashboard');const metrics=(data??{}) as Record<string,number>;return <main className="container-shell flex-1 py-10 sm:py-14"><p className="section-kicker">Visão geral</p><h1 className="mt-3 text-3xl font-semibold tracking-[-.035em] sm:text-5xl">Operação da plataforma</h1><p className="mt-3 text-slate-600">Indicadores essenciais para acompanhar o MarketPulse Local.</p>{error?<p className="mt-8 rounded-2xl bg-red-50 p-5 text-red-700">Não foi possível carregar os indicadores.</p>:<section className="mt-9 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{Object.entries(labels).map(([key,label])=><article key={key} className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><p className="text-sm text-slate-500">{label}</p><p className="mt-4 text-3xl font-semibold text-slate-950">{Number(metrics[key]??0).toLocaleString('pt-BR')}</p></article>)}</section>}</main>}
