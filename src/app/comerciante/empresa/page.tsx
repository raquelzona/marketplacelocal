import type{Metadata}from"next";import{CompanyForm}from"@/components/merchant/company-form";import{QrCodeCard}from"@/components/ui/qr-code-card";import{requireMerchantCompany}from"@/lib/merchant/data";export const metadata:Metadata={title:"Minha empresa"};
export default async function Page(){const{merchant}=await requireMerchantCompany();return <main className="container-shell flex-1 py-10 sm:py-14"><div className="mx-auto max-w-4xl"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="section-kicker">Perfil comercial</p><h1 className="mt-3 text-3xl font-semibold sm:text-4xl">Dados da empresa</h1><p className="mt-3 text-slate-600">Mantenha as informações públicas e de contato atualizadas.</p></div><Status status={merchant.verification_status}/></div><div className="mt-8 grid gap-6 lg:grid-cols-[1fr_280px]"><section className="rounded-3xl border bg-white p-6 shadow-sm sm:p-8"><CompanyForm company={merchant}/></section><div>{merchant.verification_status==="verified"?<QrCodeCard path={`/lojas/${merchant.id}`} label={merchant.nome_fantasia}/>:<p className="rounded-2xl bg-amber-50 p-5 text-sm text-amber-800">O QR público fica disponível após a verificação da empresa.</p>}</div></div></div></main>}
function Status({status}:{status:string}){const data=status==="verified"?["Verificada","bg-emerald-50 text-emerald-700"]:status==="rejected"?["Revisão necessária","bg-red-50 text-red-700"]:["Verificação pendente","bg-amber-50 text-amber-700"];return <span className={`self-start rounded-full px-3 py-1.5 text-xs font-bold ${data[1]}`}>{data[0]}</span>}
