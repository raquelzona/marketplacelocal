import type{Metadata}from"next";import{redirect}from"next/navigation";import{StoreDiscovery,type MapStore}from"@/components/consumer/store-map";import{consumerProfileComplete,requireRole}from"@/lib/auth/guards";import{createClient}from"@/lib/supabase/server";
export const metadata:Metadata={title:"Explorar lojas"};
export default async function Page(){const{profile}=await requireRole("consumer");if(!consumerProfileComplete(profile))redirect("/consumidor/onboarding");const supabase=await createClient();const{data,error}=await supabase.from("merchants").select("id,nome_fantasia,categoria,bairro,cidade,telefone,whatsapp,verification_status,latitude,longitude,sponsored").eq("cidade",profile.cidade!).order("nome_fantasia");return <main className="container-shell flex-1 py-10 sm:py-14"><p className="section-kicker">Comércio próximo</p><h1 className="mt-3 text-3xl font-semibold sm:text-4xl">Explore lojas locais</h1><p className="mt-3 text-sm text-slate-500">{profile.cidade} · sua região atual</p>{error?<p className="mt-8 rounded-2xl bg-red-50 p-5 text-red-700">Não foi possível carregar as lojas.</p>:data?.length?<StoreDiscovery stores={data as MapStore[]}/>:<div className="mt-8 rounded-3xl border border-dashed bg-white px-6 py-14 text-center"><h2 className="font-semibold">Nenhuma loja disponível nesta cidade</h2><p className="mt-2 text-sm text-slate-500">Assim que estabelecimentos forem verificados, eles aparecerão aqui.</p></div>}</main>}
