export type Forecast={current:number;forecast7:number;forecast30:number|null;growthPct:number|null;confidence:"low"|"medium"|"high";dataPoints:number}|null;
export function forecastDemand(weekly:number[]):Forecast{if(weekly.length<4)return null;const values=weekly.slice(-8);const total=values.reduce((a,b)=>a+b,0);if(total<8)return null;const n=values.length,xMean=(n-1)/2,yMean=total/n;const denominator=values.reduce((s,_,i)=>s+(i-xMean)**2,0);const slope=values.reduce((s,y,i)=>s+(i-xMean)*(y-yMean),0)/denominator;const next=Math.max(0,yMean+slope*(n-xMean));return {current:Number(yMean.toFixed(1)),forecast7:Number(next.toFixed(1)),forecast30:total>=20?Number((Math.max(0,yMean+slope*(n+1.5-xMean))*4.2857).toFixed(1)):null,growthPct:yMean?Number((slope/yMean*100).toFixed(1)):null,confidence:total>=40?"high":total>=20?"medium":"low",dataPoints:total}}
