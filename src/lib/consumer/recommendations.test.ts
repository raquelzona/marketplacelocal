import test from"node:test";import assert from"node:assert/strict";import{recommendationCategories,recommendationReason}from"./recommendations.ts";test("prioriza interesse reforçado por buscas próprias",()=>{assert.deepEqual(recommendationCategories(["Pet shop","Papelaria"],["Papelaria","Papelaria"]),["Papelaria","Pet shop"]);assert.match(recommendationReason("Papelaria",["Papelaria"]),/buscou/)});test("explica recomendação apenas por interesse",()=>assert.match(recommendationReason("Pet shop",[]),/interesse/));
