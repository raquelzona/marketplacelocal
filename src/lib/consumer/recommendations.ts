export function recommendationCategories(interests:string[],recent:(string|null)[],limit=3){const counts=new Map<string,number>();for(const value of recent)if(value)counts.set(value,(counts.get(value)??0)+3);for(const value of interests)counts.set(value,(counts.get(value)??0)+1);return [...counts].sort((a,b)=>b[1]-a[1]||a[0].localeCompare(b[0],"pt-BR")).slice(0,limit).map(([category])=>category)}
export function recommendationReason(category:string,recent:(string|null)[]){return recent.includes(category)?`Porque você buscou itens de ${category}.`:`Porque você demonstrou interesse em ${category}.`}
