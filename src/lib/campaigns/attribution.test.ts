import test from"node:test";import assert from"node:assert/strict";import{attributionBucket,normalizeAttribution}from"./attribution.ts";test("captura e limita UTMs",()=>{const a=normalizeAttribution({utm_source:"Instagram",utm_medium:"Social",utm_campaign:"Demo"});assert.deepEqual(a,{source:"instagram",medium:"social",campaign:"Demo"});assert.equal(attributionBucket(a.source),"instagram")});test("classifica direto e outros",()=>{assert.equal(attributionBucket(""),"direct");assert.equal(attributionBucket("newsletter"),"other")});
